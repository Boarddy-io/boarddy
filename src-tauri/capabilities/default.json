{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capability for the main window",
  "windows": ["main", "quick_paste", "autocomplete"],
  "permissions": [
    "core:default",
    "opener:default"
  ]
}
